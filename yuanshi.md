# 原始购房合同 + 身份证明自动翻录执行指令（PageAgent）

> 目标：在满足合规前置条件时，自动完成「身份证明」与「原始购房合同（产权证明）」的一键翻录与提交。

## 0. 固定输入数据（请勿改动）

### 0.1 身份证明表单

| 字段 | 值 |
| --- | --- |
| 卖方类型 | 个人 |
| 证件类型 | 身份证 |
| 国籍 | 中国大陆 |
| 业主姓名 | `测试` |
| 证件号码 | `42052119970425153X` |
| 有效期限-起始日期 | `2024-01-24` |
| 有效期限-结束日期 | `2026-04-23` |

### 0.2 原始购房合同（产权证明）表单

| 字段 | 值 |
| --- | --- |
| 原始购房合同登记日期 | `1986-12-08` |
| 建筑面积 | `78.88` |
| 套内面积 | `63.76` |
| 是否共有 | 是 |
| 商品房合同备案号 | `FASHFDJF2831283` |
| 行政区 | `海淀区` |
| 街道名称 | `上地八街` |
| 门牌号 | `东方电子科技大厦` |

## 1. 执行总则（必须严格遵守）

1. **禁止中途停止**：所有步骤必须连续执行至成功结束，或在失败判定后立即终止。
2. **等待策略**：任何点击/提交/切换 Tab 后，必须等待页面与弹窗完成渲染、接口请求返回、按钮可用状态稳定后再进入下一步。
3. **合规校验仅一次**：前置条件校验只做一次；通过后立即进入翻录流程，不输出任何额外提示。
4. **二次确认全自动**：出现任何二次确认弹窗，一律自动确认直至提交完成或出现失败判定。
5. **异常必须精确**：一旦判定失败，立即终止任务并抛出指定异常文本（见各步骤的失败判定）。
6. **日期选择加速规则**：选择日期时，优先在日历浮窗顶部输入框直接输入目标日期，然后直接按回车键（Enter）完成选择（无需等待或点击日期）。
7. **表单覆盖规则**：点击【一键翻录】后，无论表单是否已有历史数据，必须按「0. 固定输入数据」逐项覆盖为目标值；禁止因“表单非空/存在历史值”终止或判失败。
8. **提交后禁止复核主界面状态**：身份证明/原始购房合同提交完成后返回房源核验主界面属于正常现象。禁止基于主界面的状态文本（如仍显示“未上传”）、“上传历史”、上传人、维护人或操作按钮重新出现来判断失败或重跑前置校验。
9. **任务结束条件**：仅允许在完成身份证明与原始购房合同两个模块的提交，并分别执行完对应“失败判定”后结束任务。禁止在“符合翻录条件”后结束任务。

## 2. 进入页面

1. 点击【房源核验】。
2. 固定等待 3 秒。
3. 继续等待直到页面主要内容完全加载完成（证件模块与维护人信息可见）后，立即执行前置合规条件校验。

## 3. 前置合规条件校验（通过后立刻翻录，严禁停顿或提示）

必须同时满足以下全部条件：

> 校验必须基于**可见的页面证据**，禁止“猜测符合”。在输出“符合/不符合”结论前，必须先完成证据采集并逐条对齐规则。

### 3.1 证据采集（必须输出）

在「房源核验」页面内，分别找到并输出以下 5 项证据的**原文**（每项至少包含字段名与字段值）：

1. 权属证明：状态原文
2. 权属证明：上传人原文
3. 身份证明：状态原文
4. 身份证明：上传人原文
5. 维护人信息原文（例如：维护人姓名）

若某项证据不在首屏，允许在当前页面内滚动/展开折叠区域以获取证据。

若本次采集出现“空证据”，必须在当前页面**额外等待 5 秒**后，再完整采集一次（仅重试一次），以避免接口尚未返回导致误判。

**空证据**定义：任一证据的字段值为空，或字段值为占位符（例如：`-` `—` `暂无` `暂无数据`）。

重试后**仍无法找到任一项证据或仍为空证据**，按“不满足条件”处理。

### 3.2 判定规则（必须逐条判定）

必须同时满足以下全部条件（以证据原文为准）：

- 权属证明：状态为“未上传”，且上传人为空（无上传人/占位符如 `-` `—` 等也视为“空”）
- 身份证明：状态为“未上传”，且上传人为空（无上传人/占位符如 `-` `—` 等也视为“空”）
- 页面存在维护人信息（维护人字段可见且值非空）

失败处理：

- 任一条件不满足：必须先输出“失败原因”（明确是哪一条规则失败，以及对应的证据原文），然后终止任务，抛出异常：`不符合翻录条件`

成功处理：

- 全部满足：不得输出额外提示语；立即进入【身份证明一键翻录】并持续执行后续全部步骤，禁止在此处结束任务

## 4. 身份证明一键翻录

1. 点击页面**第一个**【核对】按钮，等待「历史证件」弹窗完全弹出。若未找到【核对】按钮，必须在当前页面**额外等待 5 秒**后重新查找并点击（仅重试一次）；重试后仍未找到则终止任务并抛出异常：`找不到核对按钮`。
2. 在「历史证件」弹窗内找到「身份证明」模块并点击【一键翻录】。点击后弹窗会消失，主页面会切换/刷新为身份证明翻录表单。固定等待 3 秒后直接开始填写（无需判断表单是否在弹窗内/是否跳转）。
3. 表单可能已存在历史数据（例如业主姓名不是 `测试`）。这不是失败条件。必须继续执行覆盖填写。
4. 按「0.1 身份证明表单」逐项覆盖填写/选择（精准覆盖，不得遗漏）：
   - 输入框字段：点击输入框 → 全选（`Ctrl/Cmd + A`）→ 删除 → 输入目标值
   - 下拉框字段：打开下拉 → 选择目标项（即使当前已是目标项也要确认选择）
5. 有效期限日期选择（适配日历输入框检索逻辑，步骤必须连贯）：
   - 起始日期框：
     1) 点击唤起日历浮窗  
     2) 找到浮窗顶部的日期输入框  
     3) 在输入框中直接输入目标日期：`2024-01-24`  
     4) 直接按回车键（Enter）完成选择
   - 结束日期框：
     1) 点击唤起日历浮窗  
     2) 找到浮窗顶部的日期输入框  
     3) 在输入框中直接输入目标日期：`2026-04-23`  
     4) 直接按回车键（Enter）完成选择
6. 点击【提交】，自动完成所有二次确认。
7. 失败判定（必须执行）：
   - 若页面出现“上传失败”相关提示：终止任务，抛出异常：`身份证明翻录失败`
   - 或等待 7 秒后，表单页面同时存在【提交】【取消】【撤销】三个按钮：终止任务，抛出异常：`身份证明翻录失败`
   - 禁止把提交后返回主界面、主界面仍显示“未上传”、出现“上传历史”、或【上传/核对】按钮重新出现当作失败依据
   - 否则：视为提交成功；立即进入【原始购房合同一键翻录】（不得做任何额外复核）

## 5. 原始购房合同一键翻录（产权证明）

1. 点击页面**第一个**【核对】按钮，等待「历史证件」弹窗完全弹出。若未找到【核对】按钮，必须在当前页面**额外等待 5 秒**后重新查找并点击（仅重试一次）；重试后仍未找到则终止任务并抛出异常：`找不到核对按钮`。
2. 在「历史证件」弹窗内找到「产权证明」模块，点击其中「原始购房合同」卡片的【一键翻录】。点击后弹窗会消失，主页面会切换/刷新为原始购房合同翻录表单。固定等待 3 秒后直接开始填写（无需判断表单是否在弹窗内/是否跳转）。
3. 表单可能已存在历史数据。这不是失败条件。必须继续执行覆盖填写。
4. 按「0.2 原始购房合同（产权证明）表单」逐项覆盖填写/选择（精准覆盖，不得遗漏）：
   - 输入框字段：点击输入框 → 全选（`Ctrl/Cmd + A`）→ 删除 → 输入目标值
   - 下拉框字段：打开下拉 → 选择目标项（即使当前已是目标项也要确认选择）
   - 日期字段：使用日历浮窗顶部输入框输入日期后按回车键（Enter）确认
5. 原始购房合同登记日期：
   1) 点击唤起日历浮窗  
   2) 找到浮窗顶部的日期输入框  
   3) 在输入框中直接输入目标日期：`1986-12-08`  
   4) 直接按回车键（Enter）完成选择
6. 点击【提交】，自动完成所有二次确认。
7. 失败判定（必须执行）：
   - 若页面出现“上传失败”相关提示：终止任务，抛出异常：`原始购房合同翻录失败`
   - 或等待 7 秒后，表单页面同时存在【提交】【取消】【撤销】三个按钮：终止任务，抛出异常：`原始购房合同翻录失败`
   - 禁止把提交后返回主界面、主界面仍显示“未上传”、出现“上传历史”、或【上传/核对】按钮重新出现当作失败依据
   - 否则：视为提交成功；判定上传成功，任务结束（不得做任何额外复核）
