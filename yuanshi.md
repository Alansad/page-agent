# 原始购房合同 + 身份证明自动翻录执行指令（PageAgent）

> 目标：在满足合规前置条件时，自动完成「身份证明」与「原始购房合同（产权证明）」的一键翻录与提交。

## 0. 固定输入数据（请勿改动）

### 0.1 身份证明表单

| 字段 | 值 |
| --- | --- |
| 卖方类型 | 个人 |
| 证件类型 | 身份证 |
| 国籍 | 中国大陆 |
| 业主姓名 | `测试` |
| 证件号码 | `42052119970425153X` |
| 有效期限-起始日期 | `2024-01-24` |
| 有效期限-结束日期 | `2026-04-23` |

### 0.2 原始购房合同（产权证明）表单

| 字段 | 值 |
| --- | --- |
| 原始购房合同登记日期 | `1986-12-08` |
| 建筑面积 | `78.88` |
| 套内面积 | `63.76` |
| 是否共有 | 是 |
| 商品房合同备案号 | `FASHFDJF2831283` |
| 行政区 | `海淀区` |
| 街道名称 | `上地八街` |
| 门牌号 | `东方电子科技大厦` |

## 1. 执行总则（必须严格遵守）

1. **禁止中途停止**：所有步骤必须连续执行至成功结束，或在失败判定后立即终止。
2. **等待策略**：任何点击/提交/切换 Tab 后，必须等待页面与弹窗完成渲染、接口请求返回、按钮可用状态稳定后再进入下一步。
3. **合规校验仅一次**：前置条件校验只做一次；通过后立即进入翻录流程，不输出任何额外提示。
4. **二次确认全自动**：出现任何二次确认弹窗，一律自动确认直至提交完成或出现失败判定。
5. **异常必须精确**：一旦判定失败，立即终止任务并抛出指定异常文本（见各步骤的失败判定）。
6. **日期选择加速规则**：选择日期时，优先在日历浮窗顶部输入框直接输入目标日期，等待约 1 秒让组件定位后，再点击选中该日期。

## 2. 进入页面

1. 点击【房源核验】。
2. 等待接口调用与页面界面完全加载完成（证件模块与维护人信息可见）后，立即执行前置合规条件校验。

## 3. 前置合规条件校验（通过后立刻翻录，严禁停顿或提示）

> 校验必须基于**可见的页面证据**，禁止“猜测符合”。在输出“符合/不符合”结论前，必须先完成证据采集并逐条对齐规则。

### 3.1 证据采集（必须输出）

在「房源核验」页面内，分别找到并输出以下 5 项证据的**原文**（每项至少包含字段名与字段值）：

1. 权属证明：状态原文
2. 权属证明：上传人原文
3. 身份证明：状态原文
4. 身份证明：上传人原文
5. 维护人信息原文（例如：维护人姓名）

若某项证据不在首屏，允许在当前页面内滚动/展开折叠区域以获取证据；**仍无法找到任一项证据**，按“不满足条件”处理。

### 3.2 判定规则（必须逐条判定）

必须同时满足以下全部条件（以证据原文为准）：

- 权属证明：状态为“未上传”，且上传人为空（无上传人/占位符如 `-` `—` 等也视为“空”）
- 身份证明：状态为“未上传”，且上传人为空（无上传人/占位符如 `-` `—` 等也视为“空”）
- 页面存在维护人信息（维护人字段可见且值非空）

失败处理：

- 任一条件不满足：必须先输出“失败原因”（明确是哪一条规则失败，以及对应的证据原文），然后终止任务，抛出异常：`不符合翻录条件`

成功处理：

- 全部满足：不得输出额外提示语；立即进入【身份证明一键翻录】

## 4. 身份证明一键翻录

1. 点击页面**第一个**【核对】按钮，等待「历史证件」弹窗完全弹出。
2. 在弹窗内找到「身份证明」模块，点击【一键翻录】并等待表单加载完成。
3. 按「0.1 身份证明表单」逐项填写/选择（精准填写，不得遗漏）。
4. 有效期限日期选择（步骤必须连贯）：
   - 起始日期框：输入并选择 `2024-01-24`
   - 结束日期框：输入并选择 `2026-04-23`
5. 点击【提交】，自动完成所有二次确认。
6. 失败判定（必须执行）：
   - 若页面出现“上传失败”相关提示：终止任务，抛出异常：`身份证明翻录失败`
   - 或等待 7 秒后，表单的【提交】按钮依然存在：终止任务，抛出异常：`身份证明翻录失败`
   - 否则：判定身份证明翻录成功，继续进入下一步

## 5. 刷新房源核验 Tab（触发页面重新加载）

1. 点击任意其他 Tab。
2. 再切回【房源核验】Tab。
3. 等待接口调用与页面界面完全加载完成后，立即进入【原始购房合同一键翻录】。

## 6. 原始购房合同一键翻录（产权证明）

1. 点击页面**第一个**【核对】按钮，等待「历史证件」弹窗完全弹出。
2. 在弹窗内找到「产权证明」模块，点击【一键翻录】并等待表单加载完成。
3. 按「0.2 原始购房合同（产权证明）表单」逐项填写/选择（精准填写，不得遗漏）。
   - 原始购房合同登记日期：输入并选择 `1986-12-08`
4. 点击【提交】，自动完成所有二次确认。
5. 失败判定（必须执行）：
   - 若页面出现“上传失败”相关提示：终止任务，抛出异常：`原始购房合同翻录失败`
   - 或等待 7 秒后，表单的【提交】按钮依然存在：终止任务，抛出异常：`原始购房合同翻录失败`
   - 否则：判定上传成功，任务结束
